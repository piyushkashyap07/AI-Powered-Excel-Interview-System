# Copy this file to .env and update with your actual MongoDB credentials

# MongoDB Configuration
# Replace the username and password with your actual MongoDB credentials
# Make sure to URL encode any special characters in username/password
MONGODB_URI=mongodb://username:password@localhost:27017/propensity_score_db

# Example with URL encoded credentials:
# If your username is "myuser@domain.com" and password is "my@password"
# Use: MONGODB_URI=mongodb://myuser%40domain.com:my%40password@localhost:27017/propensity_score_db

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/propensity_score_db

# For local MongoDB without authentication:
# MONGODB_URI=mongodb://localhost:27017/propensity_score_db 